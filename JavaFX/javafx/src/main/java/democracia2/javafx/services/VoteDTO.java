package democracia2.javafx.services;

public class VoteDTO {

  public Long
      id; // Nós percebemos que isto não faz sentido nenhum, mas não funcionou de nenhuma outra
  // forma.

  public VoteDTO(Long id) {
    this.id = id;
  }
}
